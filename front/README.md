
  # Call App with Contacts

  This is a code bundle for Call App with Contacts. The original project is available at https://www.figma.com/design/QDiw7Do22VOgvNIPtMX1CT/Call-App-with-Contacts.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  